[project]
name = "ethereum-energy-code"
version = "0.1.0"
description = "Code Ethereum Node Energy Consumption Estimation"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pandas",
    "requests",
    "joblib",
    "matplotlib",
    "huggingface-hub",
    "scikit-learn",
    "ipython",
    "ipykernel",
    "pip",
]